# Contributing

Issues and pull requests are welcome. Please keep changes scoped, add deterministic tests for
selector behavior, and run:

```bash
ruff check src tests pipelines
pytest
```

Do not commit benchmark videos, model weights, API credentials, cookies, or generated outputs.
